name: Auto Update Sports Channels

on:
  workflow_dispatch: # يتيح لك تشغيل البوت يدوياً بضغطة زر
  schedule:
    - cron: '0 * * * *' # سيعمل تلقائياً رأس كل ساعة

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # ضروري جداً للسماح للبوت بتحديث ملف database.csv
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # جلب ملفاتك من المستودع

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # تثبيت المكتبات التي يحتاجها سكربت the_beast.py
          pip install requests cloudscraper

      - name: Run The Beast
        # تأكد أن ملف البايثون في مستودعك اسمه the_beast.py
        run: python the_beast.py 

      - name: Commit and Push Changes
        run: |
          git config --local user.name "GitHub Action Bot"
          git config --local user.email "actions@github.com"
          git add database.csv
          # التحقق من وجود تغييرات حقيقية لتجنب رسائل الخطأ في GitHub
          if git diff --quiet && git diff --staged --quiet; then
            echo "لا توجد روابط جديدة أو تالفة حالياً."
          else
            git commit -m "🔄 تحديث القنوات الرياضية: $(date)"
            git push
          fi
